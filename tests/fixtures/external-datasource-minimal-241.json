{
  "key": "fixture-minimal-241",
  "displayName": "Minimal v2.4.1 datasource",
  "systemKey": "fixture-system",
  "entityType": "recordStorage",
  "resourceType": "record",
  "primaryKey": ["id"],
  "labelKey": ["id"],
  "metadataSchema": {
    "type": "object",
    "required": ["id", "externalId"],
    "properties": {
      "id": { "type": "string", "index": true },
      "externalId": { "type": "string", "index": true }
    }
  },
  "fieldMappings": {
    "attributes": {
      "id": { "expression": "{{raw.id}}" },
      "externalId": { "expression": "{{raw.externalId}}" }
    }
  },
  "exposed": {
    "schema": {
      "id": "metadata.id",
      "externalId": "metadata.externalId"
    }
  }
}
