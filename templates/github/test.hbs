name: Test Workflow
on:
  push:
    branches: [{{mainBranch}}]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test {{appName}}
        run: echo "Testing {{appName}}"